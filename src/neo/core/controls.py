# import pygame

# def controls(event, state):

#     if state == "menu":
#         if pygame.MOUSEBUTTONDOWN and event.button ==1:


# press i to get info like time and mistakes
# press c to list all controls
# press f for toggling screen width x height









"""EXTRA CODE"""
#insert this in gameState.py -> in_game_screen
# global rr, gg, bb, dr, dg, db

        
        # rr += dr
        # gg += dg
        # bb += db

        # if rr >= 255 or rr <= 0:
        #     dr = -dr
        #     rr += dr
        # if gg >= 255 or gg <= 0:
        #     dg = -dg
        #     gg += dg
        # if bb >= 255 or bb <= 0:
        #     db = -db
        #     bb += db

        # game_background_color = (rr, gg, bb)
        # globals.screen.fill(game_background_color)